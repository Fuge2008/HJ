<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorHeadTitle"
    android:orientation="vertical">

    <com.haoji.haoji.custom.HackyViewPager
        android:id="@+id/show_viewpager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"></com.haoji.haoji.custom.HackyViewPager>

    <TextView
        android:id="@+id/image_position"
        android:layout_width="wrap_content"
        android:text="1/2"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="50dp"
        android:layout_alignParentBottom="true"
        android:textColor="@color/colorWhite"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_height="40dp">

        <TextView
            android:id="@+id/show_save"
            android:layout_width="50dp"
            android:text="保存"
            android:gravity="center"
            android:textColor="@color/colorWhite"
            android:layout_height="match_parent" />
    </LinearLayout>

</RelativeLayout>